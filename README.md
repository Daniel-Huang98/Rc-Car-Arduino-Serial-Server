# Rc-Car-Arduino-Serial-Server
This repository is for tracking and storing the files for sending and receiving serial commands which in turn control the servo and motor of an ackermann steering RC car
